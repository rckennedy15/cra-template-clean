# This is a custom react-create-app template

It is designed to give the absolute bare bones needed for a simple React project.

## Usage

```
npx create-react-app <app name> --template @rckennedy15/clean
```
